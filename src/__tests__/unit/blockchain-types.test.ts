import { describe, it, expect } from '@jest/globals';
import type * as ModuleTypes from '../../services/blockchain-types.js';

describe('blockchain-types', () => {
  it('should export types', () => {
    expect(true).toBe(true);
  });
});