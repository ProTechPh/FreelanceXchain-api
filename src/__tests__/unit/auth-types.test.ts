import { describe, it, expect } from '@jest/globals';
import type * as ModuleTypes from '../../services/auth-types.js';

describe('auth-types', () => {
  it('should export types', () => {
    expect(true).toBe(true);
  });
});