import { describe, it, expect } from '@jest/globals';
import type * as ModuleTypes from '../../services/ai-types.js';

describe('ai-types', () => {
  it('should export types', () => {
    expect(true).toBe(true);
  });
});