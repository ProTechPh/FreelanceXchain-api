import { describe, it, expect } from '@jest/globals';
import type * as ModuleTypes from '../../services/blockchain/index.js';

describe('blockchain/index', () => {
  it('should export types', () => {
    expect(true).toBe(true);
  });
});