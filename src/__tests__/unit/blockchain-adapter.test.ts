import { describe, it, expect } from '@jest/globals';
import type * as ModuleTypes from '../../services/blockchain/adapter.js';

describe('blockchain/adapter', () => {
  it('should export types', () => {
    expect(true).toBe(true);
  });
});